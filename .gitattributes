# Normalise line endings in the repository so the working copy (Windows,
# CRLF from PowerShell) never shows up as a whole-file rewrite on every edit.
* text=auto eol=lf

# Binary assets must never be converted.
*.png binary
*.jpg binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
